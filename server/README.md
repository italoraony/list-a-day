# list-a-day Server

> 🚧 Coming soon — server implementation is planned for a future phase.

The server will provide REST API endpoints for the ESP32 client to fetch:
- `GET /todos` — Daily todo list
- `GET /calendar` — Today's calendar events

For now, the client includes mock data for testing.
